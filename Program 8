
class FileTooLargeError(Exception): pass

try:
    with open("data.txt", "r") as f:
        content = f.read()
        
        # ValueError check
        number = int(content) 
        print(f"Number from file: {number}")

        # Custom Error check
        if len(content) > 1000:
            raise FileTooLargeError("File is too large to process.")

except (FileNotFoundError, PermissionError) as e:
    print(f"File System Error: {e}")

except ValueError:
    print("Value Error: File content is not a valid number.")

except FileTooLargeError as e:
    print(f"Custom Error: {e}")

else:
    print("Success: File read and value converted perfectly.")
    print("File Content is:", content)

finally:
    print("Process Complete: Resources cleared.")
